"use client";

import React from "react";

const Logo = () => {
  return (
    <figure className="logo-box">
      <img src={`/logo.png`} className="logo" alt="logo" />
    </figure>
  );
};

export default Logo;
